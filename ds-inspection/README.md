# Health checks — 04 Figma SDS

Nothing has been run yet. The `ds-inspection` skill ([Brad Frost](https://github.com/bradfrost/skills), MIT) is bundled in `.claude/skills/ds-inspection/`, so it runs with no install:

> "Run the design system inspection on this repo."

Reports land here, in `reports/`, and the prioritised to-do list in `work-orders/`, as in systems 01 to 03. Keep one thing in mind when reading the result: the components are Figma's, not ours. A finding about their code is an observation, not a to-do.
